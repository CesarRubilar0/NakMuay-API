package com.example.proyecto1spring.entity;

/**
 * User.java (DEPRECATED)
 *
 * Nota: originalmente este archivo contenía la entidad `User` con mapeos a `Role`.
 * El proyecto fue unificado para usar la entidad `Usuario` como modelo único de usuario.
 * Para evitar conflictos y mantener trazabilidad, este archivo se deja como marcador
 * no público y sin anotaciones JPA.
 */

class User_DEPRECATED {
    // Clase de marcador. No es una entidad JPA y no será usada por Spring Data.
}
